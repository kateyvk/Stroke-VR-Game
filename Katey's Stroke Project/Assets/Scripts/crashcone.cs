using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class crashcone : MonoBehaviour
{
    public static float Leftwheelpwr = 0;
    public static float Rightwheelpwr = 0;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    { 
    
    }
}
